spring:

  datasource:
    url: jdbc:mysql://localhost:3306/learningplatform?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: sosoj785@gmail.com
    password: jcdryznfqnztznjj
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true

server:
  port: 8080

python:
  ai:
    url: http://localhost:8000

admin:
  secret-code: apprendreàcomprendre

app:
  admin-email: sosoj785@gmail.com

upload:
  dir: uploads/videos